/**
 * Адреса закрытой части сайта в одном месте.
 *
 * Форма входа живёт внутри админки (/admin/login), а не на корневом /login.
 * Причина не косметическая: когда адрес админки будет сменён на
 * неочевидный, форма входа переедет вместе с ним автоматически. Оставленный
 * на месте /login свёл бы всю затею на нет: сканеры ищут именно его.
 *
 * Все ссылки и редиректы должны использовать эти константы, а не строки:
 * иначе при следующем переезде часть ссылок тихо останется битой.
 */
export const ADMIN_PATH = "/admin";

export const ADMIN_LOGIN_PATH = `${ADMIN_PATH}/login`;

export const ADMIN_PASSWORD_RESET_PATH = `${ADMIN_LOGIN_PATH}/reset`;

/** Заголовок с текущим путём: proxy.ts ставит, серверные layout’ы читают. */
export const PATHNAME_HEADER = "x-pathname";
