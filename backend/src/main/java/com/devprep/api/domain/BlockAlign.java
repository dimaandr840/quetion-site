package com.devprep.api.domain;

/**
 * Выравнивание блока ответа.
 *
 * <p>Обтекания текстом сознательно нет: картинка всегда занимает отдельную строку, как в Word.
 * Обтекание потребовало бы float, который на узких экранах схлопывается и ломает порядок чтения.
 */
public enum BlockAlign {
    LEFT,
    CENTER,
    RIGHT
}
